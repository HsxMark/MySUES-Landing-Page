# 三旋翼课程表 - 前端配置
#
# 拷贝为 .env.local 后按需修改。
# 页面通过 GitHub Releases API（或自定义代理）获取最新版本信息，无需后端。
#
# ---- 必填 ----
# GitHub 仓库名称（格式：owner/repo）
NEXT_PUBLIC_GITHUB_REPO=HsxMark/MySUES
#
# ---- 可选：自定义 Releases API 代理 ----
# 设置后替代 https://api.github.com/repos/{repo}/releases
# 支持 {repo} 占位符，自动替换为 NEXT_PUBLIC_GITHUB_REPO 的值
# NEXT_PUBLIC_RELEASES_API_URL=https://example.com/api/{repo}/releases
#
# ---- 可选：自定义下载代理 ----
# 设置后点击下载时跳转到此地址，而非 GitHub browser_download_url
# 支持 {tag} 和 {file} 占位符
# NEXT_PUBLIC_DOWNLOAD_PROXY_URL=https://example.com/api/releases/download?tag={tag}&file={file}
